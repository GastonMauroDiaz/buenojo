'use strict';

angular.module('buenOjoApp')
    .controller('tabInformationController', function ($state, $scope, Course, EnrollmentCurrentUser, CourseLevelSessionEnforceEnrollment, CourseInformation) {
    // inherited scope
    
    });
